name: dart_backend_architecture
description: Production-ready Dart backend — Constructor injection, Shelf, PostgreSQL
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  postgres: ^3.5.9
  redis: ^4.0.0
  dart_nats: ^0.6.5
  dart_jsonwebtoken: ^3.3.2
  bcrypt: ^1.1.3
  zema: ^0.3.0
  logging: ^1.3.0
  dartastic_opentelemetry: ^0.9.3
  uuid: ^4.5.3
  crypto: ^3.0.7

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.30.0
  shelf_test_handler: ^2.0.2
  lints: ^6.1.0
